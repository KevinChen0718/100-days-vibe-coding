# 通知管道設定：設了哪個就推哪個，沒設就只在網頁上看（三種用法都支援）。
# 用法：把這個檔複製成 .env，填好你要的管道即可。.env 不會進 git。

# ── Discord（最簡單，不用養 bot）──
# 頻道 → 編輯頻道 → 整合 → Webhook → 新增 Webhook → 複製 Webhook 網址，貼下面：
DISCORD_WEBHOOK_URL=

# ── Telegram ──
# 1) 在 Telegram 找 @BotFather → /newbot → 取得 bot token，貼 TELEGRAM_BOT_TOKEN
# 2) 找 @userinfobot 拿你的數字 id，貼 TELEGRAM_CHAT_ID
# 3) 先對你的新 bot 隨便傳一句話（不然 bot 不能主動私訊你）
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
